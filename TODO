Things which need to be done

+make entry sensitive to enter
+cleanup
+make colors working e.g. paint it red //half fixed
+find a better solution than printing "path entry is empty" in the terminal
+better close dialog
